function countFreequency (arr) {
    let el, count
    const output = {}
    // for(let i = 0; i < arr.length; i++){
        
    //     if(output.hasOwnProperty(`${arr[i]}`)) continue

    //     el = arr[i]
    //     count = 1

    //     for(let j = i + 1; j < arr.length; j++){
    //         if(arr[i] === arr[j]){
    //            count += 1
    //         }
    //     }
    //     output[el] = count
       
    // }

    // for(i = 0; i < arr.length - 1; i++){
    //     if(output.hasOwnProperty(`${arr[i]}`)) {
    //         output[arr[i]] += 1 
    //     }else{
    //         output[arr[i]] = 1
    //     }
    // }

    // const countFree = arr.reduce((acc, el) => {
    //     if(acc.hasOwnProperty(el)){
    //         acc[el] += 1
    //     }else{
    //         acc[el] = 1
    //     }
    //     return acc
    // } , {})

}


countFreequency([7,8,7,4,5,3,1,7,8,2,1,0,3])
